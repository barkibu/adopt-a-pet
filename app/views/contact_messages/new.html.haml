%section.barkifamily
  .intro-text
    %h2 barkiNews, barkibu y barkiAdopta
  .browsers
    .browser
      .browser-top-bar
        .browser-circle
        .browser-circle
        .browser-circle
      .browser-content
    .browser
      .browser-top-bar
        .browser-circle
        .browser-circle
        .browser-circle
      .browser-content
    .browser
      .browser-top-bar
        .browser-circle
        .browser-circle
        .browser-circle
      .browser-content
.cover
.intro-text
  %h4 Contáctanos para que que te ayudemos o por cualquier otro motivo. Guau, guau..

%section
  %fieldset
    = simple_form_for @contact_message, url: contact_path do |f|
      = f.error_notification

      .form-inputs
        = f.input :name
        = f.input :email
        = f.input :subject
        = f.input :body, as: :text, input_html: {rows: 10}

      .form-actions
        = f.button :submit
